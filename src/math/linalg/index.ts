// The standard R-linear structure over packed spaces, plus the dense Matrix
// that holds metric components. LU decomposition lands here when a
// covariantly-specified metric first needs inverting.
export * from "./vector-space.ts";
export * from "./matrix.ts";
